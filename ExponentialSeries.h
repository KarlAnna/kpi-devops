#ifndef EXPONENTIAL_SERIES_H
#define EXPONENTIAL_SERIES_H

class ExponentialSeries {
public:
    /**
     * FuncA calculates the exponential series.
     * @param n: Number of terms to include in the series.
     * @return: Sum of the series.
     */
    ExponentialSeries();
    double FuncA(int n);
};

#endif
